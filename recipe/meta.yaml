{% set name = "xcube-resampling" %}
{% set version = "0.3.5" %}
{% set python_min = "3.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/xcube-dev/xcube-resampling/archive/v{{ version }}.tar.gz
  sha256: a09f185e1e57da9e1fdc79393c61a4ceca0b086b244528589af45fb6281d9803

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}
    - affine >=2.2
    - dask >=2021.6
    - dask-image >=0.6
    - numba >=0.52
    - numpy >=1.16
    - pyproj >=3.0
    - xarray >=2024.7
test:
  requires:
    - python {{ python_min }}
    - pip
  commands:
    - pip check
  imports:
    - xcube_resampling

about:
  summary: A xcube package providing low-level, dask-aware spatial resampling algorithms.
  home: https://github.com/xcube-dev/xcube-resampling
  license: MIT
  license_family: MIT
  license_file: LICENSE
  description: xcube-resampling is an xcube extension that provides spatial resampling algorithms for Earth Observation (EO) raster datasets.
  doc_url: https://xcube-dev.github.io/xcube-resampling/
  dev_url: https://github.com/xcube-dev/xcube-resampling

extra:
  recipe-maintainers:
    - clarasb
    - b-yogesh
    - konstntokas
    - pont-us
    - TejasMorbagal
    - thomasstorm
    - TonioF
