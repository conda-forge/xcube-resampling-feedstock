{% set name = "xcube-resampling" %}
{% set version = "0.3.2" %}
{% set python_min = "3.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/xcube-dev/xcube-resampling/archive/v{{ version }}.tar.gz
  sha256: ef369e82b6a31873a0c89d3a74a7b228275065451aac889ac8a01bc4b04ce127

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}
    - affine >=2.2
    - dask >=2021.6
    - dask-image >=0.6
    - numba >=0.52
    - numpy >=1.16
    - pyproj >=3.0
    - xarray >=2024.7
test:
  requires:
    - python {{ python_min }}
    - pip
  commands:
    - pip check
  imports:
    - xcube_resampling

about:
  summary: A xcube package providing low-level, dask-aware spatial resampling algorithms.
  home: https://github.com/xcube-dev/xcube-resampling
  license: MIT
  license_family: MIT
  license_file: LICENSE
  description: xcube-resampling is an xcube extension that provides spatial resampling algorithms for Earth Observation (EO) raster datasets.
  doc_url: https://xcube-dev.github.io/xcube-resampling/
  dev_url: https://github.com/xcube-dev/xcube-resampling

extra:
  recipe-maintainers:
    - clarasb
    - b-yogesh
    - konstntokas
    - pont-us
    - TejasMorbagal
    - thomasstorm
    - TonioF
